#include <stdio.h>
int main()
{
        int num;
        scanf("%d",&num);
        if (num >= 18)
                printf("Eligible for vote");
        else
                printf("Not eligible for vote");
        return 0;
}
