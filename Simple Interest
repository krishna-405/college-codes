#include <stdio.h>
int main()
{
    int a, b, c;
    printf("Enter the value of a, b and c :\n");
    scanf("%d %d %d", &a, &b, &c);
    float d;
    d = (a*b*c)/100;
    printf("%.1f", d);

    return 0;
}
